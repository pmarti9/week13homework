<li>
	{{name}}

	<button class="change-devour" data-id="{{id}}" data-newDevoured="{{devoured}}">
		{{#if devoured}}DEVOURED!{{else}}Eat this Burger{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE this burger!</button>
</li>

