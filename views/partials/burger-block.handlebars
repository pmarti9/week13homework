<li>
	{{name}}

	<button class="change-devour" data-id="{{id}}" data-newDevour="{{devour}}">
		{{#if devour}}DEVOURED!{{else}}Eat this Burger{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE this burger!</button>
</li>